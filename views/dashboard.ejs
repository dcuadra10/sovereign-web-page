<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Sovereign Empire</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Inter', sans-serif;
      min-height: 100vh;
      background: linear-gradient(135deg, #0f0c29 0%, #302b63 50%, #24243e 100%);
      color: #fff;
    }
    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 40px;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    .nav-brand { display: flex; align-items: center; gap: 12px; font-size: 1.3rem; font-weight: 700; }
    .nav-user { display: flex; align-items: center; gap: 20px; }
    .user-info { display: flex; align-items: center; gap: 10px; }
    .user-avatar { width: 40px; height: 40px; border-radius: 50%; border: 2px solid rgba(255, 255, 255, 0.3); }
    .btn {
      padding: 10px 20px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      transition: all 0.3s ease;
    }
    .btn-admin { background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); color: white; }
    .btn-logout { background: rgba(255, 255, 255, 0.1); color: white; }
    .btn:hover { transform: translateY(-2px); box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3); }
    .container { max-width: 1400px; margin: 0 auto; padding: 40px; }
    .page-header { margin-bottom: 40px; }
    .page-header h1 {
      font-size: 2.5rem;
      margin-bottom: 10px;
      background: linear-gradient(135deg, #fff 0%, #a78bfa 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    .page-header p { color: rgba(255, 255, 255, 0.6); }
    .stats-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 20px; }
    .stat-card {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border-radius: 16px;
      border: 1px solid rgba(255, 255, 255, 0.1);
      padding: 25px;
      transition: all 0.3s ease;
    }
    .stat-card:hover {
      transform: translateY(-5px);
      border-color: rgba(167, 139, 250, 0.5);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    }
    .stat-card-header { margin-bottom: 20px; }
    .stat-username { font-size: 1.2rem; font-weight: 600; }
    .stat-id { font-size: 0.8rem; color: rgba(255, 255, 255, 0.4); }
    .stat-row {
      display: flex;
      justify-content: space-between;
      padding: 8px 0;
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    }
    .stat-row:last-child { border-bottom: none; }
    .stat-label { color: rgba(255, 255, 255, 0.6); font-size: 0.9rem; }
    .stat-value { font-weight: 600; color: #a78bfa; }
    .empty-state {
      text-align: center;
      padding: 80px 40px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 16px;
      border: 1px dashed rgba(255, 255, 255, 0.2);
    }
    .empty-state-icon { font-size: 4rem; margin-bottom: 20px; }
    .search-box { margin-bottom: 30px; }
    .search-input {
      width: 100%;
      max-width: 400px;
      padding: 15px 20px;
      border-radius: 12px;
      border: 1px solid rgba(255, 255, 255, 0.2);
      background: rgba(255, 255, 255, 0.05);
      color: white;
      font-size: 1rem;
      outline: none;
    }
    .search-input:focus { border-color: #a78bfa; box-shadow: 0 0 20px rgba(167, 139, 250, 0.2); }
    .search-input::placeholder { color: rgba(255, 255, 255, 0.4); }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="nav-brand"><span></span><span>Sovereign Empire</span></div>
    <div class="nav-user">
      <% if (isAdmin) { %>
        <a href="/admin" class="btn btn-admin"> Admin Panel</a>
      <% } %>
      <div class="user-info">
        <img src="https://cdn.discordapp.com/avatars/<%= user.discordId %>/<%= user.avatar %>.png" alt="Avatar" class="user-avatar" onerror="this.src='https://cdn.discordapp.com/embed/avatars/0.png'">
        <span><%= user.username %></span>
      </div>
      <a href="/logout" class="btn btn-logout">Logout</a>
    </div>
  </nav>
  <div class="container">
    <div class="page-header">
      <h1>Kingdom Stats</h1>
      <p>View all member statistics and progress</p>
    </div>
    <div class="search-box">
      <input type="text" class="search-input" placeholder=" Search by username or ID..." id="searchInput">
    </div>
    <% if (stats.length === 0) { %>
      <div class="empty-state">
        <div class="empty-state-icon"></div>
        <h2>No Stats Available</h2>
        <p style="color: rgba(255,255,255,0.6); margin-top: 10px;">
          <% if (isAdmin) { %>Go to the Admin Panel to upload stats.<% } else { %>Stats will appear here once an admin uploads them.<% } %>
        </p>
      </div>
    <% } else { %>
      <div class="stats-grid" id="statsGrid">
        <% stats.forEach(stat => { %>
          <div class="stat-card" data-username="<%= stat.username?.toLowerCase() || '' %>" data-id="<%= stat.governor_id %>">
            <div class="stat-card-header">
              <div class="stat-username"><%= stat.username %></div>
              <div class="stat-id">ID: <%= stat.governor_id %></div>
            </div>
            <div class="stat-row">
              <span class="stat-label"> Highest Power</span>
              <span class="stat-value"><%= Number(stat.highest_power || 0).toLocaleString() %></span>
            </div>
            <div class="stat-row">
              <span class="stat-label"> Deads (T4+T5)</span>
              <span class="stat-value"><%= Number(stat.deads || 0).toLocaleString() %></span>
            </div>
            <div class="stat-row">
              <span class="stat-label"> Kill Points</span>
              <span class="stat-value"><%= Number(stat.total_kill_points || 0).toLocaleString() %></span>
            </div>
            <div class="stat-row">
              <span class="stat-label"> Resources Gathered</span>
              <span class="stat-value"><%= Number(stat.resources_gathered || 0).toLocaleString() %></span>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>
  <script>
    const searchInput = document.getElementById('searchInput');
    const statsGrid = document.getElementById('statsGrid');
    if (searchInput && statsGrid) {
      searchInput.addEventListener('input', (e) => {
        const query = e.target.value.toLowerCase();
        statsGrid.querySelectorAll('.stat-card').forEach(card => {
          const matches = card.dataset.username.includes(query) || card.dataset.id.includes(query);
          card.style.display = matches ? 'block' : 'none';
        });
      });
    }
  </script>
</body>
</html>
