<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Sovereign Empire</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background: #0f0c29; color: white; display: flex; align-items: center; justify-content: center; height: 100vh; margin: 0; }
        .card { background: rgba(255,255,255,0.05); padding: 40px; border-radius: 12px; border: 1px solid rgba(255,255,255,0.1); width: 100%; max-width: 400px; text-align: center; }
        h1 { margin-bottom: 20px; font-size: 1.8rem; background: linear-gradient(135deg, #fff 0%, #a78bfa 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .btn { display: block; width: 100%; padding: 12px; border-radius: 6px; text-decoration: none; font-weight: 600; color: white; margin-bottom: 15px; border: none; cursor: pointer; font-size: 1rem; }
        .btn-discord { background: #5865F2; }
        .btn-discord:hover { background: #4752C4; }
        .btn-primary { background: linear-gradient(135deg, #667eea, #764ba2); }
        .divider { margin: 20px 0; opacity: 0.5; font-size: 0.9rem; display: flex; align-items: center; gap: 10px; }
        .divider::before, .divider::after { content: ''; flex: 1; height: 1px; background: rgba(255,255,255,0.2); }
        input { width: 100%; padding: 12px; margin-bottom: 10px; border-radius: 6px; border: 1px solid rgba(255,255,255,0.2); background: rgba(0,0,0,0.3); color: white; box-sizing: border-box; }
        .links { font-size: 0.9rem; margin-top: 15px; }
        a { color: #a78bfa; text-decoration: none; }
        .err { background: rgba(220, 38, 38, 0.2); border: 1px solid rgba(220, 38, 38, 0.5); color: #fca5a5; padding: 10px; border-radius: 6px; margin-bottom: 15px; }
    </style>
</head>
<body>
    <div class="card">
        <h1>Welcome Back</h1>
        <% if (error) { %><div class="err"><%= error %></div><% } %>
        
        <form action="/login" method="POST">
            <input type="email" name="email" placeholder="Email" required>
            <input type="password" name="password" placeholder="Password" required>
            <button class="btn btn-primary">Login with Email</button>
        </form>

        <div class="divider">OR</div>

        <a href="/auth/discord" class="btn btn-discord">Login with Discord</a>
        
        <div class="links">
            New here? <a href="/register">Register</a> <br>
            <a href="/">Back to Home</a>
        </div>
    </div>
</body>
</html>
